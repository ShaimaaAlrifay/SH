# Loaded automatically by `vite build` (production mode) — no secrets here,
# just the public URL of the deployed Gemini/Groq proxy (see server.js /
# render.yaml). Local dev ignores this file and falls back to
# http://localhost:3001 instead (see AreebLanding.jsx's API_BASE).
VITE_API_URL=https://sh-htbh.onrender.com
